<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vietnam.lottery.business.basicIndicators.mapper.KeepStatisticsMapper">

    <select id="isKeep" resultType="java.lang.Integer">
        select count(0)
        from keep_statistics
        where create_by = #{userId}
          and date_format(create_date, '%Y-%m-%d') = date_format(now(), '%Y-%m-%d')
    </select>
</mapper>

